{
  "id": "ch3-ex01-select",
  "title": "Your first SELECT",
  "type": "sql",
  "prompt_md": "Read every movie from the `movies` table. Return only the `id` and `title` columns (skip `rating`).",
  "schema_key": "ticketpay",
  "starter_code": "SELECT id, title FROM movies;",
  "hints": [
    "`SELECT` lists the columns you want; `FROM` names the table.",
    "Separate column names with commas: `SELECT id, title`."
  ],
  "expected_query_result": [
    {"id": 1, "title": "Dune"},
    {"id": 2, "title": "Inside Out"},
    {"id": 3, "title": "Tenet"}
  ],
  "order_sensitive": false,
  "reference_solution": "SELECT id, title FROM movies;",
  "xp": 15,
  "concept_tags": ["select"]
}
